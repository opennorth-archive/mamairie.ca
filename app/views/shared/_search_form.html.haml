= form_tag search_path, method: :get do
  %fieldset
    .clearfix
      = label_tag :q, t('search.label')
      = text_field_tag :q, params[:q], placeholder: t('search.placeholder'), class: 'large'
      = submit_tag t('search.submit'), class: 'btn primary large'
